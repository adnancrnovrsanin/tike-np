const FavoritesList = (products) => {
  return (
    <div className="grid grid-cols-3 gap-4">
      AAAAAAAAAAA
      {/* {products.map((product) => (
                <div key={product.name} className="bg-[#FFF4E0] rounded-xl border-2 border-[#262626] shadow-[8px_8px_0px_1px_#FFBE99] p-6">
                    <div className="relative h-52">
                        <Image src={product.image} alt={product.name} layout="fill" objectFit="cover" />
                    </div>
                    <h2 className="text-2xl font-bold text-[#262626] mt-4">{product.name}</h2>
                    <p className="text-lg text-[#262626] mt-2">${product.price}</p>
                </div>
            ))} */}
    </div>
  );
};

export default FavoritesList;
